apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
    - host: marketplace
      http:
        paths:
          - path: /api/v1/
          - backend:
              service:
                name: marketplace-service
                port:
                  number: 9000
            pathType: