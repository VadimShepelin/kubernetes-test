apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
    - host: marketplace
      http:
        paths:
          - backend:
              service:
                name: marketplace-service

            pathType: