apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name  }}-redis-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: standard