apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name  }}-rate-deploy
  labels:
    app: {{ .Release.Name  }}-rate
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name  }}-rate
  template:
    metadata:
      labels:
        app: {{ .Release.Name  }}-rate
    spec:
      containers:
        - name: rate
          image: kocichca152/rate:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name  }}-rate-service
  labels:
    app: {{ .Release.Name  }}-rate-service
spec:
  selector:
    app: {{ .Release.Name  }}-rate
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
